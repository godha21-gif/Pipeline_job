pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Example: run Maven build
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                // Example: run unit tests
            }
        }
    }
}
